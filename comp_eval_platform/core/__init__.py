"""Core Django app: shared domain models, task/step state machine, and the
engine glue that every competition variant reuses unchanged."""

default_app_config = "comp_eval_platform.core.apps.CoreConfig"
