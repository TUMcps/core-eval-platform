"""comp-eval-platform: core engine for a modular competition-evaluation platform.

Competitions (VNN-COMP, ARCH-COMP, …) are plugins that register a ``Competition`` subclass into the
core registry; the engine itself is variant-agnostic. See ``docs/unified-platform.md``.
"""

__version__ = "0.1.0"
